# Reference: Real-world engineering scenario
# Topic: API Design (REST vs gRPC) - Tier 1 System Architecture

## Questions:
1. When would you choose gRPC over REST for a microservice-to-microservice communication, and what are the serialization benefits?
2. How do you handle versioning in a RESTful API without breaking existing client implementations?
3. What is the impact of HTTP/2 on gRPC performance compared to standard REST implementations?

# TODO (Answer in 3-4 sentences each)
1. I do not really know what gRPC is so I would not know when to use it over REST.
2. I would define the output of services and ensure they are consistent between versions.
3. No clue